# 双图谱使用流程图

## 🎯 快速参考

### 三个图谱的核心定位

| 图谱 | 回答问题 | 作用层级 | 使用顺序 | 产出 |
|------|---------|---------|---------|------|
| **因果图谱** | 为什么（Why） | 理论层 | 第1步 | 研究假设 + 因果路径 |
| **变量映射器** | 如何连接（Bridge） | 实现层 | 第2步 | 数据字段 + 计算方法 |
| **方法图谱** | 怎么做（How） | 方法层 | 第3步 | 方法案例 + 参数配置 |

---

## 📊 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户输入                              │
│  "研究研发投资对技术影响力的影响，考虑技术多样性的中介作用"    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   Strategist Agent 启动                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
        ┌───────────────────┴───────────────────┐
        ↓                                       ↓
┌──────────────────┐                  ┌──────────────────┐
│  提取关键词       │                  │  加载数据列名     │
│  ["研发投资",     │                  │  ["申请人",      │
│   "技术多样性",   │                  │   "IPC分类",     │
│   "中介效应"]     │                  │   "序号", ...]   │
└──────────────────┘                  └──────────────────┘
        ↓                                       ↓
┌─────────────────────────────────────────────────────────────┐
│                  Step 1: 因果图谱查询                        │
│                  (理论层 - 回答"为什么")                      │
└─────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  CausalGraphQuery.suggest_hypotheses(user_goal)              │
│  ↓                                                            │
│  输出:                                                        │
│  • 研究假设: V03 → V16 (正向, large, 14篇文献)               │
│  • 中介路径: V03 → V09 → V16 (4条路径)                       │
│  • 理论机制: "研发投资通过技术多样性影响技术影响力"            │
└──────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│                  Step 2: 变量映射                            │
│                  (实现层 - 连接理论和数据)                    │
└─────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  VariableMapper.get_required_columns_for_hypothesis()        │
│  ↓                                                            │
│  输入: V03, V09, V16                                          │
│  ↓                                                            │
│  映射:                                                        │
│  • V03 → ['申请(专利权)人', '序号']                          │
│  • V09 → ['IPC分类号']                                       │
│  • V16 → ['被引用专利']                                      │
│  ↓                                                            │
│  输出:                                                        │
│  • 所需字段: ['申请人', '序号', 'IPC', '被引用']             │
│  • 计算方法: {V03: "COUNT/COUNT", V09: "Entropy", ...}       │
│  • 数据可用性: {V03: ✓, V09: ✓, V16: ✗缺失}                 │
└──────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│                  Step 3: 方法图谱检索                        │
│                  (方法层 - 回答"怎么做")                      │
└─────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  Neo4jConnector.retrieve_best_practices(keywords)            │
│  ↓                                                            │
│  检索关键词: ["研发投资", "技术多样性", "中介效应"]           │
│  ↓                                                            │
│  输出:                                                        │
│  • 案例1: "专利分析方法" (4步流程)                           │
│    - Step 1: 数据收集                                        │
│    - Step 2: 专利地图/可视化                                 │
│    - Step 3: 技术机会识别                                    │
│    - Step 4: 内部能力评估                                    │
│  • 案例2: "文献计量分析" (6步流程)                           │
│    - Step 1: 文献检索                                        │
│    - Step 2: 引文分析                                        │
│    - ...                                                     │
└──────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│                  Step 4: 整合生成DAG任务图                   │
│                  (整合三个图谱的信息)                         │
└─────────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────────┐
│  Strategist._generate_dag_blueprint()                        │
│  ↓                                                            │
│  整合信息:                                                    │
│  • 因果图谱 → 研究假设 (V03→V09→V16)                        │
│  • 变量映射 → 数据字段 + 计算方法                            │
│  • 方法图谱 → 方法名称 + 参数配置                            │
│  ↓                                                            │
│  生成任务:                                                    │
│  ┌────────────────────────────────────────────────┐          │
│  │ Task 1: 数据准备                                │          │
│  │ - 输入: Excel文件                               │          │
│  │ - 输出: data_summary.json                       │          │
│  │ - 字段: ['申请人', 'IPC', '被引用']  ← 变量映射 │          │
│  └────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────┐          │
│  │ Task 2: 计算V03 (研发投资强度)                  │          │
│  │ - 方法: 聚合分析  ← 方法图谱                    │          │
│  │ - 计算: COUNT/COUNT  ← 变量映射                 │          │
│  │ - 输出: rd_investment.csv                       │          │
│  └────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────┐          │
│  │ Task 3: 计算V09 (技术多样性)                    │          │
│  │ - 方法: 熵计算  ← 方法图谱                      │          │
│  │ - 计算: Shannon Entropy  ← 变量映射             │          │
│  │ - 输出: tech_diversity.csv                      │          │
│  └────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────┐          │
│  │ Task 4: 计算V16 (技术影响力)                    │          │
│  │ - 方法: 引用分析  ← 方法图谱                    │          │
│  │ - 计算: COUNT(前向引用)  ← 变量映射             │          │
│  │ - 输出: tech_impact.csv                         │          │
│  └────────────────────────────────────────────────┘          │
│  ┌────────────────────────────────────────────────┐          │
│  │ Task 5: 中介效应检验                            │          │
│  │ - 假设: V03→V09→V16  ← 因果图谱                │          │
│  │ - 方法: Baron & Kenny  ← 方法图谱              │          │
│  │ - 输出: mediation_analysis.json                 │          │
│  └────────────────────────────────────────────────┘          │
└──────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│                   输出: DAG任务图                            │
│  {                                                           │
│    "research_objective": "...",                              │
│    "task_graph": [task_1, task_2, task_3, task_4, task_5]   │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│                 Methodologist Agent 处理                     │
│  (将每个任务转换为技术规格/伪代码)                            │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│                   Coding Agent 处理                          │
│  (将技术规格转换为可执行代码)                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔍 详细步骤说明

### Step 1: 因果图谱查询

**输入**:
- 用户目标: "研究研发投资对技术影响力的影响"
- 关键词: ["研发投资", "技术影响力"]

**处理**:
```python
causal_query = CausalGraphQuery()

# 1. 搜索相关变量
vars = causal_query.search_variables_by_keyword("研发投资")
# 找到: V03_rd_investment

vars = causal_query.search_variables_by_keyword("技术影响力")
# 找到: V16_tech_impact

# 2. 查找因果路径
direct_path = causal_query.find_direct_path("V03", "V16")
# 找到: V03 → V16 (正向, large, 14篇文献)

mediation_paths = causal_query.get_mediation_paths("V03", "V16")
# 找到: 4条中介路径
```

**输出**:
```json
{
  "hypotheses": [
    {
      "type": "direct",
      "path": "V03 → V16",
      "effect": "positive",
      "evidence": 14
    },
    {
      "type": "mediation",
      "path": "V03 → V09 → V16",
      "mediator": "技术多样性",
      "evidence": 8
    }
  ]
}
```

### Step 2: 变量映射

**输入**:
- 变量列表: ["V03", "V09", "V16"]
- 可用列名: ["申请人", "IPC分类", "序号", ...]

**处理**:
```python
mapper = VariableMapper()

# 1. 获取每个变量的数据字段
for var in ["V03", "V09", "V16"]:
    fields = mapper.get_data_fields(var)
    calc = mapper.get_calculation_method(var)
    
# 2. 检查数据可用性
availability = mapper.check_data_availability(var, available_columns)

# 3. 生成任务配置
config = mapper.generate_task_config(var)
```

**输出**:
```json
{
  "V03_rd_investment": {
    "data_fields": ["申请(专利权)人", "序号"],
    "calculation": "COUNT(专利) / COUNT(DISTINCT 申请人)",
    "is_available": true
  },
  "V09_tech_diversity": {
    "data_fields": ["IPC分类号"],
    "calculation": "Shannon Entropy",
    "is_available": true
  },
  "V16_tech_impact": {
    "data_fields": ["被引用专利"],
    "calculation": "COUNT(前向引用)",
    "is_available": false,
    "missing": ["被引用专利"]
  }
}
```

### Step 3: 方法图谱检索

**输入**:
- 关键词: ["研发投资", "技术多样性", "中介效应"]

**处理**:
```python
neo4j = Neo4jConnector()

all_cases = []
for keyword in keywords:
    cases = neo4j.retrieve_best_practices(keyword, limit=2)
    all_cases.extend(cases)
```

**输出**:
```json
{
  "cases": [
    {
      "paper": "A patent-based approach...",
      "steps": [
        {"step": 1, "method": "数据收集"},
        {"step": 2, "method": "专利地图"},
        {"step": 3, "method": "技术机会识别"}
      ]
    }
  ]
}
```

### Step 4: 整合生成任务图

**输入**:
- 因果假设: V03 → V09 → V16
- 变量映射: {V03: [...], V09: [...], V16: [...]}
- 方法案例: [{...}, {...}]

**处理**:
```python
# Strategist 整合信息
blueprint = {
  "task_graph": [
    {
      "task_id": "task_2",
      "question": "如何计算研发投资强度？",  # 来自因果图谱
      "input_variables": [],
      "output_variables": ["V03_rd_investment"],
      "implementation_config": {
        "data_fields": ["申请人", "序号"],  # 来自变量映射
        "calculation": "COUNT/COUNT",  # 来自变量映射
        "method": "聚合分析"  # 来自方法图谱
      }
    }
  ]
}
```

**输出**: 完整的DAG任务图

---

## 🎯 关键决策树

```
用户输入
    ↓
是否需要理论支撑？
    ├─ 是 → 使用因果图谱
    │       ↓
    │   找到因果路径？
    │       ├─ 是 → 继续
    │       └─ 否 → 探索性分析
    │
    └─ 否 → 跳过因果图谱
        ↓
使用了因果图谱？
    ├─ 是 → 使用变量映射器
    │       ↓
    │   数据字段可用？
    │       ├─ 是 → 继续
    │       └─ 否 → 推荐替代变量
    │
    └─ 否 → 跳过变量映射器
        ↓
需要方法参考？
    ├─ 是 → 使用方法图谱
    │       ↓
    │   找到相关案例？
    │       ├─ 是 → 继续
    │       └─ 否 → 使用通用方法
    │
    └─ 否 → 跳过方法图谱
        ↓
生成任务图
```

---

## 📋 使用检查清单

### 使用因果图谱前
- [ ] 用户目标明确
- [ ] 需要理论支撑
- [ ] 涉及变量间关系

### 使用变量映射器前
- [ ] 已使用因果图谱
- [ ] 需要数据字段
- [ ] 需要计算方法

### 使用方法图谱前
- [ ] 需要方法参考
- [ ] 不确定用什么算法
- [ ] 需要参数配置

### 生成任务图前
- [ ] 至少使用了一个图谱
- [ ] 信息已整合
- [ ] 任务依赖关系清晰

---

## ⚠️ 常见错误

### 错误 1: 跳过因果图谱
```
❌ 错误:
用户目标 → 直接使用方法图谱 → 生成任务

✓ 正确:
用户目标 → 因果图谱 → 变量映射 → 方法图谱 → 生成任务
```

### 错误 2: 不检查数据可用性
```
❌ 错误:
变量映射 → 直接生成任务 → 执行失败（字段缺失）

✓ 正确:
变量映射 → 检查数据可用性 → 调整方案 → 生成任务
```

### 错误 3: 忽略因果假设
```
❌ 错误:
方法图谱 → 生成任务（缺乏理论支撑）

✓ 正确:
因果图谱 → 方法图谱 → 生成任务（有理论支撑）
```

---

**文档版本**: 1.0  
**创建日期**: 2026-01-19  
**用途**: 快速参考和流程指导
